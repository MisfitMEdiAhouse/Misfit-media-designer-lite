name: CoffeeAndAJoint Temporary DNS Cleanup
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/caj-vtest-run.yml'
permissions:
  contents: read
jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Remove temporary vtest DNS records
        run: |
          curl -fsS -X POST 'https://misfit-media-designer-lite.vercel.app/api/admin/caj-vtest-4db219?action=cleanup-test&confirm=caj-495-cutover-20260820'
