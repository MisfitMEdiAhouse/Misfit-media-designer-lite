const ZONE='coffeeandajoint.co';
const BASE='https://api.godaddy.com/v3/domains/zones';
function headers(){const token=process.env.GODADDY_PAT;if(!token)throw new Error('GODADDY_PAT missing');return{Authorization:`Bearer ${token}`,Accept:'application/json'}}
async function readAll(){const u=new URL(`${BASE}/${ZONE}/dns-records`);u.searchParams.set('pageSize','100');u.searchParams.set('totalRequired','true');const r=await fetch(u,{headers:headers()});const text=await r.text();let data={};try{data=text?JSON.parse(text):{}}catch{data={raw:text}}if(!r.ok)throw new Error(`GoDaddy ${r.status}`);return Array.isArray(data?.items)?data.items:(Array.isArray(data)?data:[])}
export default async function handler(req,res){res.setHeader('Cache-Control','no-store');res.setHeader('X-Robots-Tag','noindex');if(req.method!=='GET')return res.status(405).json({ok:false});try{const rows=await readAll();const relevant=rows.filter(r=>r.name==='@'||r.name==='www'||String(r.name).startsWith('_openai')||String(r.name).startsWith('_redirect')).map(r=>({recordId:r.recordId,type:r.type,name:r.name,data:r.data,ttl:r.ttl}));return res.status(200).json({ok:true,zone:ZONE,relevant})}catch(e){return res.status(500).json({ok:false,error:String(e?.message||e)})}}
