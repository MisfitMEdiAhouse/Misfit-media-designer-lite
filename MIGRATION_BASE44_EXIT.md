# Misfit Mediahouse Base44 Exit

Target runtime: GitHub + Vercel + Supabase.

The replacement build removes the Base44 Vite plugin, Base44 SDK authentication wrapper, Base44 runtime API calls, Base44-hosted favicon dependency, and direct public links to legacy Base44 demo URLs.

Production cutover must not occur until the Vercel preview passes homepage, proof, enterprise-ai, creator-commerce, command authentication, lead capture, sitemap, robots, and unknown-path 404 checks.
