(() => {
  const ACT_API = 'https://cibcxqrqiqvzpardbdrw.supabase.co/functions/v1/site-activation';
  const REF_STORAGE = 'misfit_client_referral_v1';
  const CUSTOMER_REF_STORAGE = 'misfit_customer_referral_v1';
  const SESSION_KEY = 'misfit_referral_session_v1';
  const REF_DAYS = 30;
  const CUSTOMER_REF_DAYS = 90;
  const cleanCode = (value) => { const code=String(value||'').trim().toLowerCase(); return /^[a-z0-9][a-z0-9-]{1,78}$/.test(code)?code:''; };
  const refUrl = (path, query={}) => { const u=new URL('/api/client-referral',location.origin); u.searchParams.set('path',path); Object.entries(query).forEach(([k,v])=>v!=null&&u.searchParams.set(k,String(v))); return u.toString(); };
  function sessionId(){ try{let v=sessionStorage.getItem(SESSION_KEY);if(!v){v=crypto?.randomUUID?.()||`${Date.now()}-${Math.random().toString(16).slice(2)}`;sessionStorage.setItem(SESSION_KEY,v);}return v;}catch(_){return `${Date.now()}-${Math.random().toString(16).slice(2)}`;} }
  function detectClientSiteKey(){ const explicit=document.querySelector('meta[name="misfit-client-site-key"]')?.content||document.documentElement.dataset.misfitClientSiteKey||window.MISFIT_CLIENT_SITE_KEY;if(cleanCode(explicit))return cleanCode(explicit);if(document.querySelector('nav[aria-label="Golden Essence navigation"]'))return 'golden-essence';return ''; }
  function saveAgencyReferral(code){try{localStorage.setItem(REF_STORAGE,JSON.stringify({code,expires_at:Date.now()+REF_DAYS*86400000}));}catch(_){}}
  function loadAgencyReferral(){try{const i=JSON.parse(localStorage.getItem(REF_STORAGE)||'{}');if(!i?.code||!i?.expires_at||Number(i.expires_at)<Date.now()){localStorage.removeItem(REF_STORAGE);return '';}return cleanCode(i.code);}catch(_){return '';}}
  function saveCustomerReferral(siteKey,code){try{localStorage.setItem(CUSTOMER_REF_STORAGE,JSON.stringify({site_key:siteKey,code,expires_at:Date.now()+CUSTOMER_REF_DAYS*86400000}));}catch(_){}}
  function loadCustomerReferral(siteKey){try{const i=JSON.parse(localStorage.getItem(CUSTOMER_REF_STORAGE)||'{}');if(!i?.code||i?.site_key!==siteKey||!i?.expires_at||Number(i.expires_at)<Date.now()){if(i?.site_key===siteKey)localStorage.removeItem(CUSTOMER_REF_STORAGE);return '';}return cleanCode(i.code);}catch(_){return '';}}
  function post(path,body){fetch(refUrl(path),{method:'POST',headers:{'content-type':'application/json'},keepalive:true,body:JSON.stringify(body)}).catch(()=>{});}
  function trackAgency(eventType,code,extra={}){if(!code)return;post('track',{referral_code:code,event_type:eventType,session_id:sessionId(),landing_path:`${location.pathname}${location.search}`.slice(0,400),source:'misfitmediahouse_web',...extra});}
  function trackCustomer(siteKey,eventType,code,action=''){if(!siteKey||!code)return;post('customer-track',{site_key:siteKey,referrer_code:code,event_type:eventType,action,session_id:sessionId(),landing_path:`${location.pathname}${location.search}`.slice(0,400),source:'client_site'});}
  const params=new URLSearchParams(location.search);const inboundAgency=cleanCode(params.get('ref'));if(inboundAgency){saveAgencyReferral(inboundAgency);trackAgency('landing',inboundAgency);}const activeAgencyReferral=()=>inboundAgency||loadAgencyReferral();
  function decorateStripeLinks(){const code=activeAgencyReferral();if(!code)return;document.querySelectorAll('a[href^="https://buy.stripe.com/"]').forEach(a=>{try{const u=new URL(a.href);if(!u.searchParams.get('client_reference_id')){u.searchParams.set('client_reference_id',`ref_${code}`);u.searchParams.set('utm_source','client_referral');a.href=u.toString();}}catch(_){}});}
  const siteKey=detectClientSiteKey();const inboundCustomer=cleanCode(params.get('client_ref'));if(siteKey&&inboundCustomer){saveCustomerReferral(siteKey,inboundCustomer);trackCustomer(siteKey,'landing',inboundCustomer,'visit');}const activeCustomerReferral=()=>siteKey?(inboundCustomer||loadCustomerReferral(siteKey)):'';
  document.addEventListener('click',(event)=>{const stripe=event.target.closest?.('a[href^="https://buy.stripe.com/"]');const agency=activeAgencyReferral();if(stripe&&agency)trackAgency('checkout_click',agency,{offer:(stripe.textContent||'').trim().slice(0,120)});const customer=activeCustomerReferral();if(!siteKey||!customer)return;const actionable=event.target.closest?.('a[href^="tel:"],a[href^="mailto:"],a[href*="book" i],a[href*="appointment" i],[data-referral-lead-action]');if(!actionable)return;const href=actionable.getAttribute?.('href')||'';const action=actionable.dataset?.referralLeadAction||(href.startsWith('tel:')?'phone':href.startsWith('mailto:')?'email':'booking');trackCustomer(siteKey,'lead_action',customer,action);},true);
  async function addClientReferralTool(){const key=detectClientSiteKey();if(!key||document.getElementById('misfit-client-referral-link'))return;try{const r=await fetch(`${ACT_API}/status?site=${encodeURIComponent(key)}`,{cache:'no-store'});const s=await r.json();if(!r.ok||!s.activated)return;const nav=document.querySelector('nav[aria-label="Golden Essence navigation"]');if(nav){const row=nav.firstElementChild||nav;const link=document.createElement('a');link.id='misfit-client-referral-link';link.href=`/client-referral.html?site=${encodeURIComponent(key)}`;link.textContent='Referral Center';link.style.cssText='display:flex;align-items:center;justify-content:center;border-radius:8px;padding:8px 4px;color:#e7b65d;text-decoration:none;font-size:8px;font-weight:700;text-transform:uppercase;letter-spacing:.03em;white-space:nowrap;';row.appendChild(link);if(getComputedStyle(row).display==='grid')row.style.gridTemplateColumns=`repeat(${row.children.length},minmax(0,1fr))`;return;}const link=document.createElement('a');link.id='misfit-client-referral-link';link.href=`/client-referral.html?site=${encodeURIComponent(key)}`;link.textContent='Referral Center';link.style.cssText='position:fixed;right:14px;bottom:14px;z-index:1200;border:1px solid rgba(226,165,60,.45);border-radius:999px;background:#052629;color:#efc56d;padding:10px 13px;text-decoration:none;font:700 10px/1 Inter,system-ui;text-transform:uppercase;letter-spacing:.08em;box-shadow:0 10px 30px rgba(0,0,0,.35)';document.body.appendChild(link);}catch(_){}}
  decorateStripeLinks();addClientReferralTool();new MutationObserver(()=>{decorateStripeLinks();addClientReferralTool();}).observe(document.documentElement,{childList:true,subtree:true});
})();
